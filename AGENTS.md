## Base44 Dev Environment

- Run with: `docker compose -f docker-compose.base44.yml up -d`
- Node 22 base image, source bind-mounted at `/app`, `node_modules` in a named volume.
- Dev server: `next dev --webpack -H 0.0.0.0 -p 3000` (live reload via watchpack polling).
- No external services, database, or secrets required — frontend-only project.
- `next.config.ts` has `allowedDevOrigins` wired to `BASE44_PUBLIC_HOST_SUFFIX` so the preview origin can access dev assets/HMR.
- Verify: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/` should return 200.

# سياق المشروع

قالب موقع ويب لمطعم، قابل لإعادة الاستخدام، Frontend فقط، مخصص للمطاعم المصرية.
ملف `BRD.md` هو المصدر الأساسي والمرجعي لمتطلبات المنتج، وهيكل الصفحة، وتدفقات المستخدم، والحالات، ونطاق العمل.

## القواعد الأساسية

* قراءة `BRD.md` قبل تنفيذ أي مهمة غير بسيطة.
* فحص المشروع الحالي والملفات ذات الصلة قبل إجراء أي تعديل.
* إعادة استخدام المكونات، والأنماط، والأدوات المساعدة، والأنماط البرمجية الموجودة عند الملاءمة.
* الحفاظ على التعديلات مركّزة وبأقل قدر ممكن.
* عدم تعديل ملفات أو سلوكيات غير ذات صلة.
* عدم اختراع متطلبات أو إضافة ميزات غير معرّفة في `BRD.md` إلا إذا طُلب ذلك صراحة.
* عدم إضافة اعتماديات (dependencies) إلا إذا كانت ضرورية فعلاً.
* الحفاظ على محتوى كل مطعم قابلاً للتهيئة ومنفصلاً عن واجهة المستخدم القابلة لإعادة الاستخدام.
* عدم إدخال Backend، أو قاعدة بيانات، أو تسجيل دخول (Authentication)، أو مدفوعات، أو ميزات إدارية، أو تحليلات (Analytics) إلا إذا طُلب ذلك صراحة.

## قواعد الواجهة الأمامية (Frontend)

* استخدام نفس تقنيات وأنماط المشروع الحالية.
* تفضيل مكونات React بسيطة وسهلة الصيانة، وتجنّب التجريد غير الضروري.
* الحفاظ على حالة الواجهة (UI State) واضحة ومحلية قدر الإمكان.
* الحفاظ على السلوك الحالي عند تعديل أي ميزة.
* تجنّب تكرار المنطق البرمجي وتكرار أنماط الواجهة.
* استخدام HTML دلالي (Semantic) وعناصر تفاعلية قابلة للوصول (Accessible).
* توفير حالات مناسبة للتحميل، والفراغ، والتعطيل، والتحقق، والأخطاء عند الحاجة.

## التحقق من الصحة ومعالجة الأخطاء

* التحقق من صحة المدخلات المطلوبة قبل الانتقال للخطوة التالية في أي تدفق.
* منع الإجراءات غير الصحيحة بدلاً من الاعتماد فقط على رسائل الخطأ.
* التعامل بسلاسة مع السلة الفارغة، والأصناف غير المتاحة، والكميات غير الصحيحة، والحقول المطلوبة الناقصة، وحالات الطلب غير الصحيحة.
* الحفاظ على مدخلات المستخدم الصحيحة عند فشل التحقق من صحة البيانات الأخرى.
* استخدام رسائل واضحة ومختصرة بالعامية المصرية لعرض حالات التحقق والأخطاء للمستخدم.
* عدم عرض أخطاء تقنية خام للمستخدم أبداً.
* في حال حدوث فشل غير متوقع في الواجهة، يجب أن يفشل بسلاسة، مع توفير إجراء مفيد للتعافي عند الإمكان.

## قواعد واجهة وتجربة المستخدم (UI/UX)

* العربية أولاً، واتجاه الصفحة من اليمين لليسار (RTL).
* النصوص الظاهرة للمستخدم يجب أن تكون بعامية مصرية طبيعية.
* تصميم مصمم أولاً للموبايل؛ استخدام مقاس 390×844 كمرجع تصميم أساسي.
* التحقق من السلوك المتجاوب (Responsive) على المقاسات 360×800، و390×844، و412×915، وكذلك على التابلت وسطح المكتب.
* الحفاظ على توجه بصري دافئ، بسيط، ودود، متزن، ويركز على الطعام.
* إعطاء الأولوية للوضوح، وسهولة الاستخدام، وتدفق الطلب، على حساب التأثيرات الزخرفية.
* تجنّب أنماط التصميم النمطية التي تبدو مولّدة بالذكاء الاصطناعي، والتدرجات اللونية الزائدة، وتأثير الزجاج (Glassmorphism)، والبطاقات كبيرة الحجم بشكل مبالغ فيه، والظلال الزائدة، والحركة غير الضرورية، وأقسام Hero الضخمة بلا داعٍ.
* مراعاة خط مقروء، وتباين ألوان كافٍ، وعناصر تحكم مناسبة للمس، ومراعاة تفضيلات تقليل الحركة (Reduced Motion).

## المحتوى والبيانات

* الحفاظ على بيانات المطعم، وأقسام المنيو، وأصناف المنيو، والآراء، وبيانات التواصل، ومواعيد العمل، والهوية البصرية قابلة للتهيئة.
* عدم ترميز محتوى خاص بعميل معين بشكل ثابت داخل المكونات القابلة لإعادة الاستخدام.
* عدم اختراع معلومات فعلية عن المطعم، أو أسعار، أو آراء، أو عناوين، أو أرقام هواتف، أو تفاصيل عمل أخرى أبداً.

## سير عمل التطوير

الفهم ← الفحص ← التخطيط ← التنفيذ ← التحقق من الصحة ← المراجعة النهائية.

قبل الانتهاء من أي مهمة:

* التأكد من أن التنفيذ يطابق `BRD.md`.
* اختبار تدفق المستخدم الرئيسي المتأثر بالتعديل.
* مراجعة حالات الفراغ، وعدم الصحة، والتعطيل، والأخطاء ذات الصلة.
* التحقق من عدم وجود أي تراجع (Regressions) في السلوك الحالي.
* التحقق من الواجهة المعنية على الموبايل وسطح المكتب.
* الإبلاغ عمّا تم تعديله وما تم التحقق منه.

عدم توسيع نطاق العمل بشكل غير معلن (بصمت).
